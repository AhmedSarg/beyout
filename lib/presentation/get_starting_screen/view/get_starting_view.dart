import 'package:flutter/material.dart';

import 'widgets/get_starting_body.dart';

class GetStartingScreen extends StatelessWidget {
  const GetStartingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: GetStartingBody());
  }
}
